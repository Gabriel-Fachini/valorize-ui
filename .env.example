# ============================================
# GOOGLE CLOUD RUN - ENVIRONMENT VARIABLES
# ============================================
# Copy this file and rename to .env.production
# Fill in the values according to your environment
# ============================================

# ============================================
# DASHBOARD ENVIRONMENT VARIABLES
# ============================================

# API Base URL - Used for all API requests
VITE_API_BASE_URL=https://api.valorize.com

# API URL - Can be different from base URL if using proxy
VITE_API_URL=https://api.valorize.com

# ============================================
# LANDING PAGE ENVIRONMENT VARIABLES
# ============================================

# Public site URL - Used for canonical URLs and SEO
PUBLIC_SITE_URL=https://valorize.com

# ============================================
# GOOGLE CLOUD CONFIGURATION
# ============================================

# GCP Project ID (get from: gcloud config get-value project)
GCP_PROJECT_ID=valorize-ui-123456

# Cloud Run Region
# Options: us-central1, us-east1, southamerica-east1, europe-west1, asia-east1
# See: https://cloud.google.com/run/docs/locations
GCP_REGION=us-central1

# ============================================
# CLOUD RUN SERVICE SETTINGS (Optional)
# ============================================
# These can be configured in cloudbuild.yaml or deploy scripts

# Dashboard settings
DASHBOARD_MEMORY=512Mi
DASHBOARD_CPU=1
DASHBOARD_MIN_INSTANCES=0
DASHBOARD_MAX_INSTANCES=10

# Landing page settings
LANDING_MEMORY=256Mi
LANDING_CPU=1
LANDING_MIN_INSTANCES=0
LANDING_MAX_INSTANCES=5

# ============================================
# NOTES
# ============================================
#
# 1. Never commit .env files with real values to Git!
#    Add .env.production to .gitignore
#
# 2. For local development, use .env.local
#
# 3. For Cloud Build triggers, set variables in:
#    Console > Cloud Build > Triggers > [your-trigger] > Substitution Variables
#
# 4. For Cloud Run services, update environment variables with:
#    gcloud run services update SERVICE_NAME --set-env-vars KEY=VALUE
#
# 5. To view current environment variables:
#    gcloud run services describe SERVICE_NAME --format='value(spec.template.spec.containers[0].env)'
#
