---
import Container from '../ui/Container.astro'
import Card from '../ui/Card.astro'
import CheckIcon from '../icons/CheckIcon.astro'
import { pricing } from '../../constants/pricing'
---

<section id="pricing" class="py-20 bg-white">
  <Container>
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 mb-6">
        Planos que <span class="text-gradient">crescem com você</span>
      </h2>
      <p class="text-xl text-gray-600 leading-relaxed">
        Escolha o plano ideal para o tamanho da sua empresa. Sem surpresas, sem taxas ocultas.
      </p>
    </div>
    
    <!-- Pricing Cards -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {pricing.map((plan) => (
        <Card 
          variant="pricing" 
          className={`relative p-8 ${plan.popular ? 'ring-2 ring-brand-primary-500 shadow-xl scale-105' : ''}`}
        >
          <!-- Popular Badge -->
          {plan.popular && (
            <div class="absolute -top-4 left-1/2 transform -translate-x-1/2">
              <span class="bg-gradient-brand text-white px-4 py-2 rounded-full text-sm font-medium">
                Mais Popular
              </span>
            </div>
          )}
          
          <!-- Plan Header -->
          <div class="text-center mb-8">
            <h3 class="text-2xl font-bold text-gray-900 mb-2">
              {plan.name}
            </h3>
            <div class="mb-4">
              <span class="text-4xl font-bold text-gray-900">{plan.price}</span>
              {plan.period && (
                <span class="text-gray-600 ml-2">{plan.period}</span>
              )}
            </div>
          </div>
          
          <!-- Features List -->
          <ul class="space-y-4 mb-8">
            {plan.features.map((feature) => (
              <li class="flex items-start space-x-3">
                <div class="flex-shrink-0 mt-0.5">
                  <CheckIcon className="text-green-500" size={16} />
                </div>
                <span class="text-gray-700">{feature}</span>
              </li>
            ))}
          </ul>
          
          <!-- CTA Button -->
          <button 
            class={`w-full py-4 px-6 rounded-xl font-semibold transition-all duration-200 ${
              plan.popular 
                ? 'bg-gradient-brand text-white hover:bg-gradient-brand-hover shadow-lg hover:shadow-xl' 
                : 'bg-gray-100 text-gray-900 hover:bg-gray-200 border border-gray-300'
            }`}
            data-plan={plan.name.toLowerCase()}
          >
            {plan.cta}
          </button>
        </Card>
      ))}
    </div>
    
    <!-- Additional Info -->
    <div class="mt-16 text-center">
      <div class="bg-gray-50 rounded-2xl p-8 max-w-4xl mx-auto">
        <h3 class="text-xl font-semibold text-gray-900 mb-4">
          Por que escolher a Valorize?
        </h3>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-8">
          <div class="text-center">
            <div class="text-2xl mb-2">✅</div>
            <h4 class="font-semibold text-gray-900 mb-2">14 dias grátis</h4>
            <p class="text-sm text-gray-600">Teste completo sem compromisso</p>
          </div>
          
          <div class="text-center">
            <div class="text-2xl mb-2">🔒</div>
            <h4 class="font-semibold text-gray-900 mb-2">Dados seguros</h4>
            <p class="text-sm text-gray-600">LGPD e certificações de segurança</p>
          </div>
          
          <div class="text-center">
            <div class="text-2xl mb-2">💬</div>
            <h4 class="font-semibold text-gray-900 mb-2">Suporte dedicado</h4>
            <p class="text-sm text-gray-600">Ajuda quando você precisar</p>
          </div>
        </div>
        
        <!-- Enterprise Contact -->
        <div class="mt-8 p-6 bg-white rounded-xl border-2 border-dashed border-gray-300">
          <h4 class="font-semibold text-gray-900 mb-2">
            Empresa com mais de 200 colaboradores?
          </h4>
          <p class="text-gray-600 mb-4">
            Temos planos especiais e funcionalidades exclusivas para grandes empresas.
          </p>
          <a 
            href="/contato"
            class="inline-flex items-center justify-center px-6 py-3 text-brand-primary-600 font-semibold rounded-lg border border-brand-primary-600 hover:bg-brand-primary-50 transition-colors"
          >
            Falar com Especialista
            <svg class="ml-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
        </div>
      </div>
    </div>
  </Container>
</section>